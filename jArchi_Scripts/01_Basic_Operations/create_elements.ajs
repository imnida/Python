/*
 * Create Basic ArchiMate Elements
 *
 * This script demonstrates how to create various ArchiMate elements
 * in the current model.
 */

console.log("Creating ArchiMate Elements...");

// Get the current model
var model = $("model").first();

if (!model) {
    console.log("No model found. Please open a model first.");
} else {
    // Create Business Layer elements
    var businessActor = model.createElement("business-actor", "Customer");
    var businessRole = model.createElement("business-role", "Sales Manager");
    var businessProcess = model.createElement("business-process", "Order Processing");
    var businessService = model.createElement("business-service", "Order Service");

    // Create Application Layer elements
    var appComponent = model.createElement("application-component", "CRM System");
    var appService = model.createElement("application-service", "Customer Data Service");
    var appInterface = model.createElement("application-interface", "REST API");
    var dataObject = model.createElement("data-object", "Customer Record");

    // Create Technology Layer elements
    var node = model.createElement("node", "Application Server");
    var device = model.createElement("device", "Web Server");
    var systemSoftware = model.createElement("system-software", "Database Server");
    var artifact = model.createElement("artifact", "Configuration File");

    // Create Motivation elements
    var stakeholder = model.createElement("stakeholder", "Board of Directors");
    var driver = model.createElement("driver", "Increase Revenue");
    var goal = model.createElement("goal", "Improve Customer Satisfaction");
    var requirement = model.createElement("requirement", "System must be available 24/7");

    // Create Implementation elements
    var workPackage = model.createElement("work-package", "CRM Implementation");
    var deliverable = model.createElement("deliverable", "User Documentation");

    console.log("✓ Elements created successfully!");
    console.log("Business Layer: " + businessActor.name + ", " + businessRole.name);
    console.log("Application Layer: " + appComponent.name + ", " + appService.name);
    console.log("Technology Layer: " + node.name + ", " + device.name);
    console.log("Motivation: " + stakeholder.name + ", " + goal.name);
}
